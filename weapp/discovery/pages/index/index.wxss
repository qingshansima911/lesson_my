page {
  position: relative;
  width: 100vw; /*相对单位 viewport width 视窗宽度*/
  height: 100vh;/* 相对单位  viewport height 视窗高度*/
    /* display: flex;
    align-items: center;
    justify-content: space-between; */
}

.container {
  position: absolute;
  top: 50%;
  left: 50%;
  /* margin: 50% 50%; */
  transform: translate(-50%, -50%); 
  width: 80%;
  height: 80%;
  border: 1px solid red;
  /* margin: 0px auto; */
  overflow: hidden;
  border-radius: 5px;
  text-align: center;
}
.title {
  color: red;
  font-size: 20px;
}
